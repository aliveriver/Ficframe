# 安全与隐私说明

FicFrame 是一个本地运行的工具，但当你启用 LLM 增强或图片生成功能时，部分内容可能会被发送到你配置的第三方 API。

## 哪些内容可能离开本机

根据你使用的功能，以下内容可能会发送给你配置的服务商：

- 小说片段、章节摘要、场景摘要
- 人物设定、角色外貌、人设特征
- 生图 prompt
- 上传的人设参考图

FicFrame 无法控制第三方服务商如何存储、处理或使用这些内容。处理未公开小说、商业稿件、私有人设或敏感图片前，请先确认对应服务商的隐私政策和使用条款。

## 本地密钥

API key 可能保存在以下文件中：

```text
.env
.ficframe/providers.json
```

这两个路径已经加入 `.gitignore`。请不要把它们提交到公开仓库，也不要在 issue、截图、日志或聊天中泄露自己的 API key。

## 日志

服务端日志默认写入：

```text
outputs/logs/
```

Web 顶部的“导出日志”会生成一个 zip 日志包。FicFrame 会尝试对 API key 和常见 token 格式做脱敏，但日志包仍然可能包含：

- 运行元数据
- 文件名
- 第三方服务商返回的错误信息
- 可选的 run 文件，例如 `pipeline.json`、`storyboard.md`、`prompts.md`
- 小说片段、角色设定或 prompt 的一部分

公开分享日志包前，请先解压检查，删除你不想公开的小说正文、人设细节、prompt、图片或其他私有内容。

## 漏洞反馈

如果你发现安全问题，请不要在公开 issue 中直接贴出敏感细节、API key、私有日志或可复现的攻击载荷。

建议做法：

1. 先提交一个简短 issue，说明“发现了安全问题，需要私下沟通”。
2. 不要在公开内容里包含密钥、隐私数据或攻击细节。
3. 与维护者确认私下沟通方式后，再提供完整复现信息。

## 内容责任

使用 FicFrame 时，你需要自行确认：

- 你有权处理上传的小说、人设文档和参考图。
- 你的 LLM 与图片生成服务商允许处理这些内容。
- 你理解把未公开作品、私人设定或敏感图片发送给第三方 API 的风险。
- 你在公开日志、截图、示例和 issue 前已经完成必要脱敏。

## 建议

- 开源仓库中只保留可公开的最小示例。
- 不要把真实小说、人设参考图、生成图或 API 配置提交到 Git。
- 反馈 bug 时优先使用“导出日志”，但分享前仍要人工检查。
- 如果你在商业项目中使用 FicFrame，请根据自己的合规要求审查第三方模型服务。
